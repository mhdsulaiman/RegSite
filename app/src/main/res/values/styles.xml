<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="StartTheme" parent="Theme.AppCompat">
        <item name="android:windowBackground">@drawable/regsite_launch_screen</item>
    </style>

    <style name="AccountTheme" parent="Theme.AppCompat.Light.NoActionBar"/>

    <style name="PartyTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="PartyTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
